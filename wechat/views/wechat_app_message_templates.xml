<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!--
        企微「图文素材消息(mpnews)」正文HTML的预览页面。
        独立页面（不继承 web.layout），在浏览器新标签打开，用 sandbox iframe 隔离渲染正文，
        避免正文中的样式/脚本影响预览页外壳，也避免任何脚本在已登录会话中被执行
        （iframe 只加 allow-same-origin，不加 allow-scripts，正文里的 <script> 不会被执行）。

        重要说明（预览的局限性）：
        企业微信对 mpnews 正文的处理是"服务端清洗 + 客户端渲染"两段式的，浏览器预览只能还原
        "客户端渲染"这一段的近似效果，无法还原服务端清洗逻辑，包括但不限于：
        - 会自动去除 <script>，<style> 整块样式在实际客户端上的支持并不可靠（建议全部使用行内 style）；
        - 正文中 <img> 的 src 必须是"上传图文消息内的图片"接口返回的URL，其它外链图片会被屏蔽显示；
        - 企微 PC 客户端（Windows/Mac）内置的浏览器内核普遍比手机端更旧，对 flex/grid/position:fixed
          等较新的CSS支持较差，样式差异往往就出在"手机上正常，电脑客户端上错位"，建议改用
          table 或简单 div + 行内 style 的"邮件式"排版以获得最一致的效果；
        - 不支持自定义 @font-face/webfont。
    -->
    <template id="wechat_app_message_preview_page" name="企微图文消息正文预览">
<html>
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1"/>
    <title>正文预览 - <t t-esc="message.name"/></title>
    <style>
        * { box-sizing: border-box; }
        body {
            margin: 0;
            font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", Helvetica,
                "PingFang SC", "Hiragino Sans GB", "Microsoft Yahei", Arial, sans-serif;
            background: #f2f2f2;
            color: #222;
        }
        .toolbar {
            position: sticky;
            top: 0;
            z-index: 10;
            background: #07C160;
            color: #fff;
            padding: 10px 16px;
            display: flex;
            align-items: center;
            gap: 10px;
            flex-wrap: wrap;
        }
        .toolbar .title { font-weight: 600; margin-right: auto; }
        .toolbar button {
            background: rgba(255,255,255,0.15);
            border: 1px solid rgba(255,255,255,0.5);
            color: #fff;
            padding: 5px 12px;
            border-radius: 4px;
            cursor: pointer;
            font-size: 13px;
        }
        .toolbar button.active { background: #fff; color: #07C160; font-weight: 600; }
        .tips {
            margin: 12px 16px 0 16px;
            padding: 10px 14px;
            background: #fff7e6;
            border: 1px solid #ffd591;
            border-radius: 6px;
            font-size: 13px;
            line-height: 1.7;
            color: #7c5a00;
        }
        .tips ul { margin: 6px 0 0 0; padding-left: 18px; }
        .stage {
            display: flex;
            justify-content: center;
            padding: 20px 16px 40px 16px;
        }
        #previewFrame {
            width: 390px;
            height: 80vh;
            max-width: 100%;
            border: 1px solid #ddd;
            border-radius: 12px;
            background: #fff;
            box-shadow: 0 4px 16px rgba(0,0,0,0.08);
        }
        #previewFrame.viewport-tablet { width: 680px; }
        #previewFrame.viewport-desktop { width: 100%; max-width: 900px; border-radius: 4px; }
    </style>
</head>
<body>
    <div class="toolbar">
        <span class="title">正文预览：<t t-esc="message.name"/></span>
        <button type="button" class="active" onclick="setViewport(this, 'mobile')">手机宽度</button>
        <button type="button" onclick="setViewport(this, 'tablet')">平板宽度</button>
        <button type="button" onclick="setViewport(this, 'desktop')">桌面宽度</button>
    </div>
    <div class="tips">
        以下为浏览器近似渲染，不会模拟企业微信服务端的清洗逻辑，发布前请以真实设备收到的消息为准：
        <ul>
            <li>企微只可靠支持内联(inline) style，&lt;style&gt;整块样式和外部CSS/JS在客户端上不保证生效；</li>
            <li>正文图片必须使用"上传图文消息内的图片"接口得到的URL，否则会被企微屏蔽；</li>
            <li>企微PC客户端内置浏览器内核较旧，flex/grid/position:fixed等较新布局兼容性差，建议用table或div+行内style排版；</li>
            <li>不支持自定义字体(@font-face)、视频标签等。</li>
        </ul>
    </div>
    <div class="stage">
        <iframe id="previewFrame" class="viewport-mobile" sandbox="allow-same-origin"></iframe>
    </div>
    <script type="text/plain" id="rawContent"><t t-esc="message.content or ''"/></script>
    <script><![CDATA[
        (function () {
            var raw = document.getElementById('rawContent').textContent;
            var doc = '<!DOCTYPE html><html><head><meta charset="utf-8"/>' +
                '<meta name="viewport" content="width=device-width, initial-scale=1"/>' +
                '<style>' +
                'body{margin:0;padding:16px;font-family:-apple-system,BlinkMacSystemFont,' +
                '"Helvetica Neue",Helvetica,"PingFang SC","Hiragino Sans GB","Microsoft Yahei",' +
                'Arial,sans-serif;font-size:17px;line-height:1.75;color:#333;word-break:break-word;}' +
                'img{max-width:100% !important;height:auto !important;}' +
                'table{max-width:100%;border-collapse:collapse;}' +
                'a{color:#576b95;}' +
                '</style>' +
                '</head><body>' + raw + '</body></html>';
            document.getElementById('previewFrame').srcdoc = doc;
        })();
        function setViewport(btn, mode) {
            var frame = document.getElementById('previewFrame');
            frame.classList.remove('viewport-mobile', 'viewport-tablet', 'viewport-desktop');
            frame.classList.add('viewport-' + mode);
            var buttons = btn.parentNode.querySelectorAll('button');
            for (var i = 0; i < buttons.length; i++) { buttons[i].classList.remove('active'); }
            btn.classList.add('active');
        }
    ]]></script>
</body>
</html>
    </template>

</odoo>
