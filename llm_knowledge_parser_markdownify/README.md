# LLM Knowledge Parser - Markdownify

Adds Markdownify-based HTML conversion to the legacy record-backed parser pipeline.

```bash
python3 -m pip install markdownify
odoo-bin -d your_database -i llm_knowledge_parser_markdownify
```
