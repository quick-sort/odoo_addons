# LLM Knowledge Retriever - HTTP

Compatibility addon for attachment-backed resources using `retriever="http"`.
New URL uploads use native URL resources and an extractor addon instead.

```bash
python3 -m pip install requests markdownify
odoo-bin -d your_database -i llm_knowledge_retriever_http
```
