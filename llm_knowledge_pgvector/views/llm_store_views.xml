<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="llm_store_view_form_inherit_pgvector" model="ir.ui.view">
        <field name="name">llm.store.form.inherit.pgvector</field>
        <field name="model">llm.store</field>
        <!-- `llm_store` was merged into `llm_knowledge`; the base view moved
             with it. Same xmlid the llm_pg and llm_qdrant adapters inherit. -->
        <field name="inherit_id" ref="llm_knowledge.llm_store_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='service']" position="after">
                <field
          name="pgvector_index_method"
          invisible="service != 'pgvector_local'"
        />
            </xpath>
        </field>
    </record>
</odoo>
