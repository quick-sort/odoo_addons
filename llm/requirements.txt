# Generated by scripts/generate_requirements.py -- do not edit by hand.
#
# Runtime dependencies for the llm addon. Odoo does not read this file; it
# checks `external_dependencies` in __manifest__.py, which is where these come
# from. Kept in sync from the manifest and from module-level imports.
#
# Install with:  pip install -r llm/requirements.txt

# Declared in __manifest__.py external_dependencies.
pydantic>=2.0.0
mcp
requests
emoji
markdown2
pyyaml
jsonschema

# Not declared in __manifest__.py. Each line says why it is here
# anyway; the '# undeclared' ones belong in the manifest so Odoo
# checks them too.
markupsafe  # undeclared
psycopg2  # undeclared
packaging  # needed by Odoo to parse the manifest
