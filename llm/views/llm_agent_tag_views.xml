<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- Form View for LLM Assistant Tag -->
    <record id="llm_agent_tag_view_form" model="ir.ui.view">
        <field name="name">llm.agent.tag.form</field>
        <field name="model">llm.agent.tag</field>
        <field name="arch" type="xml">
            <form string="Agent Tag">
                <sheet>
                    <group>
                        <field name="name" />
                        <field name="color" widget="color_picker" />
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Tree View for LLM Assistant Tag -->
    <record id="llm_agent_tag_view_tree" model="ir.ui.view">
        <field name="name">llm.agent.tag.tree</field>
        <field name="model">llm.agent.tag</field>
        <field name="arch" type="xml">
            <list>
                <field name="name" />
                <field name="color" widget="color" invisible="1" />
            </list>
        </field>
    </record>

    <!-- Search View for LLM Assistant Tag -->
    <record id="llm_agent_tag_view_search" model="ir.ui.view">
        <field name="name">llm.agent.tag.search</field>
        <field name="model">llm.agent.tag</field>
        <field name="arch" type="xml">
            <search string="Search Agent Tags">
                <field name="name" />
            </search>
        </field>
    </record>

    <!-- Action for Tags -->
    <record id="llm_agent_tag_action" model="ir.actions.act_window">
        <field name="name">Agent Tags</field>
        <field name="res_model">llm.agent.tag</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new agent tag
            </p>
            <p>
                Tags help classify and filter your agents.
            </p>
        </field>
    </record>
</odoo>
