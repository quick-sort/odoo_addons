# LLM Knowledge Parser - PyMuPDF

Adds PDF text and image extraction to the legacy record-backed parser pipeline.

```bash
python3 -m pip install PyMuPDF
odoo-bin -d your_database -i llm_knowledge_parser_pymupdf
```
